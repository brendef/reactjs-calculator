const Screen = (props) => {
    return(
      <div>
        <div>
          {props.number}
        </div>
        <div>
          {props.answer}
        </div>
      </div>
    );
}

export default Screen;
